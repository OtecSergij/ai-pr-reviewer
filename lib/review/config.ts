// Конфигурация фичи ревью — пороги и лимиты в одном месте.

// Максимум изменённых файлов в PR; больше — отклоняем (стоимость и размер контекста).
export const MAX_CHANGED_FILES = 15;

// Потолок шагов агентного цикла — страховка от неограниченной стоимости.
export const MAX_STEPS = 60;
